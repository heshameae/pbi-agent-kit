{
  "name": "pbi-agent-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Power BI semantic-model beta for Claude Code — modeling MCP server, skills, subagents, and guardrails.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build && node scripts/write-build-marker.mjs",
    "test": "pnpm -r test",
    "verify:release": "node scripts/verify-release-artifact.mjs",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@9.15.0"
}
