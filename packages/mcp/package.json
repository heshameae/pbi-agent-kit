{
  "name": "pbi-modeling-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing governed Power BI semantic-model tools.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "pbi-modeling-mcp": "./dist/server.js"
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "pbi-core": "workspace:*",
    "zod": "^3.23.8"
  }
}
