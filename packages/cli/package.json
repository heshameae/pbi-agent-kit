{
  "name": "pbi-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Commander CLI wrapper around pbi-core for PBIR report authoring.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "pbi-ts": "./dist/main.js"
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "pbi-core": "workspace:*"
  }
}
