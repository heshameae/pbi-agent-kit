# System Improvements

Living log of behavior and logic hardening work. Keep entries short. Update or delete stale entries when a newer decision supersedes them.

## Active Improvements

| Area | Improvement | Why | Reference files |
|---|---|---|---|
| Live model workflow | Model writes are live-first. Omit `folderPath` when Desktop is open; live changes appear immediately and persist with Ctrl+S. Folder mode is offline fallback only. | Prevent stale disk edits, close/reopen workflows, and edits landing in the wrong model. | `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md`; `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts` |
| Live model discovery freshness | User-facing model reads use fresh live snapshots by default, redact connection strings, and return remediation guidance when targeted live tables are missing. Ctrl+S is not presented as the only remedy; agents should check target instance, Power Query apply/materialization, live refresh, and reopen-after-disk-edit cases. | Prevent stale cache/target confusion from hiding newly-added Desktop tables and giving misleading persistence-only advice. | `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-list-tools.test.ts`; `packages/mcp/tests/model-driver.test.ts`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md` |
| Modeling-only beta surface | Dashboard/report/page/visual/PBIR authoring is unavailable in this modeling-only beta. Scope is enforced by directory isolation, not a manifest allowlist. The marketplace installs `.claude-plugin/plugin.json`, which carries only `mcpServers: ./.mcp.json` (`PBI_MCP_SURFACE=modeling`) and relies on default scanning of `skills/` and `agents/`. A manifest `skills` list ADDS to the default scan (it does not exclude), so only the 5 modeling skills live under `skills/` and only the 3 modeling agents under `agents/`; report/dashboard/PBIR skills+agents live under `skills-report/`/`agents-report/` and dev/meta skills under `skills-internal/`, none of which the beta plugin scans. No explicit `agents` file-path array is used — it suppressed default discovery and loaded zero agents in this Claude Code version. The MCP launcher rebuilds when source/package inputs are newer than untracked `dist/server.js`, so pulled tool changes are not silently stale after restart. `claude plugin details` is the launch gate (must show Skills (5) + Agents (3)). `UserPromptSubmit`/`UserPromptExpansion`/`PreToolUse` hooks remain a defense-in-depth backstop that refuses report prompts, stale report skill/agent expansion, and report/PBIR tool calls. | A manifest allowlist does not scope a plugin: only what sits under the auto-scanned `skills/`/`agents/` dirs is shipped, so report behavior must be physically separated, not just omitted from a list. Verify the effective inventory with `claude plugin details`, never the manifest array, and keep compiled MCP output fresh from source. | `.mcp.json`; `.mcp.full.json`; `scripts/start-mcp.mjs`; `.claude-plugin/plugin.json`; `.claude-plugin/marketplace.json`; `README.md`; `hooks/hooks.json`; `hooks/scripts/guard-modeling-beta-scope.mjs`; `skills-report/`; `agents-report/`; `skills-internal/`; `packages/mcp/tests/claude-contracts.test.ts`; `packages/mcp/tests/plugin-inventory.test.ts` |
| No raw file surgery | Agents must not hand-edit TMDL/PBIP/Report/CSV artifacts by default; hooks allow only explicit repo fixtures/templates, block shell/Node file surgery, block alias-agnostic direct Microsoft MCP write bypasses, and docs point users at the wrapper rather than a raw peer server. Unsupported operations are reported as unsupported. | Desktop does not reload disk edits into the running model; raw edits bypass wrapper gates. | `README.md`; `skills/pbi-init-config/SKILL.md`; `skills-report/pbi-layout/SKILL.md`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md`; `hooks/hooks.json`; `hooks/scripts/guard-no-python-powerbi-ops.mjs`; `packages/mcp/tests/claude-contracts.test.ts`; `packages/mcp/src/server.ts` |
| No Python operations | Python is blocked for pbi-mcp-ts operations, including absolute/venv interpreter paths, data probing, CRLF patches, TMDL rewrites, and CSV inspection. Use MCP tools or repo-native TypeScript/Node. | Prevent ad hoc scripts from bypassing deterministic model/report gates. | `hooks/hooks.json`; `hooks/scripts/guard-no-python-powerbi-ops.mjs`; `packages/mcp/tests/claude-contracts.test.ts`; `AGENTS.md`; `CLAUDE.md` |
| Date table planning | Existing Date table edits, marking, and diagnosis require `pbi_model_plan_date_table`; new Date/Calendar tables use `pbi_date_table_create_governed`. Literal calendar bounds and volatile anchors like `TODAY()` are rejected unless an explicit future horizon policy is supplied. | Prevent hardcoded ranges, missing years, blank rows, and date logic drifting with system date. | `packages/core/src/modeling/date-grain-plan.ts`; `packages/core/src/modeling/bpa.ts`; `packages/core/tests/modeling/date-grain-plan.test.ts`; `packages/core/tests/modeling/bpa.test.ts`; `skills/reviewing-models/references/check-catalog.md` |
| Governed Date table create | New Date/Calendar tables use `pbi_date_table_create_governed`, which asks for Date policy and refresh-before-probe policy when ambiguous, proves fact-date evidence before any write, refreshes before proof only when `refreshBeforeProbe:true` is explicit, derives the complete fact-date set before generating dynamic fact-anchored DAX, caps future-horizon ends at the explicit horizon, hardens generated-column metadata, marks the table, and creates Date relationships only when the Date grain write plan proves them safe. Blocked/incomplete Date proof stops the workflow: do not use `pbi_dax_query` as a fallback or provide manual DAX. | Replace prompt-generated `CALENDAR(DATE(...), DATE(...))`, `TODAY()` anchors, manual DAX entry, manual metadata edits, ungated refresh/retry loops, too-narrow calendars, and unsafe monthly-target Date relationships. | `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md` |
| Date grain planning | Before asking target grain/day/month/year questions, target/budget/forecast workflows require proof first. Actuals/targets-style joins start with `pbi_model_plan_actuals_targets_join`; other grain-sensitive workflows call `pbi_model_plan_date_grain`. Only `writePlan` / `plainSumSafe` are action-safe; `observedGrain` is evidence for observable date grain, while allocation and missing-target behavior remain user semantics after proof. | Prevent unnecessary grain questions, monthly/daily grain assumptions, repeated target values, and DateTime relationships that pass date-only coverage but fail exact joins. | `packages/mcp/src/server.ts`; `packages/core/src/modeling/date-grain-plan.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `packages/core/tests/modeling/date-grain-plan.test.ts`; `packages/mcp/tests/claude-contracts.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/data-analyst.md`; `agents/model-builder.md`; `agents/model-reviewer.md`; `skills/modeling-semantic-model/SKILL.md`; `skills/modeling-semantic-model/references/grain.md` |
| Live refresh tooling | `pbi_model_refresh` refreshes/processes the live model when Import data or calculated tables must materialize and refresh is explicitly approved. If approval is missing, agents ask for refresh authorization and stop; Ctrl+S is persistence only. | Prevent workflows from stopping midway, asking users to click Desktop Refresh unnecessarily, or refreshing unexpectedly; use the MCP refresh path only with an explicit freshness policy. | `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md` |
| Date relationship gates | Temporal relationship creates/updates/activations require governed Date table coverage, exact date-grain proof, explicit future-horizon policy, and single-direction filtering. | Prevent invalid date joins, unapproved future calendar padding, and ambiguous temporal filters. | `packages/mcp/src/server.ts`; `packages/core/src/modeling/date-grain-plan.ts`; `packages/core/src/modeling/relationship-check.ts`; `packages/core/tests/modeling/relationship-check.test.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts` |
| Live DAX probe result handling | `daxQuery` normalizes the wrapped modeling MCP's columnar Execute envelope (`data.columns` or nested `data.results[].tables[]` + positional `rows` array-of-arrays in `content[].text`, no `structuredContent`) into rows keyed by column name; malformed tabular rows throw instead of reading as empty. Engine errors returned without MCP `isError` (`success:false` / non-tabular text) throw; probe blocks/incomplete paths surface `rowCount`/`columns`/`sampleRow` diagnostics, and governed create proves facts before writing. Generated probe DAX must stay parenthesis-balanced (regression-tested) since the driver is mocked in unit tests and never executes it. | The live DAX result shape is not the public Execute-Queries REST array-of-objects shape, so unproven assumptions silently returned zero rows and masked engine/syntax errors, blocking date-table, mark-as-date, and temporal relationship proofs. | `packages/mcp/src/model-bridge/model-driver.ts`; `packages/core/src/modeling/date-grain-plan.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/model-list-tools.test.ts`; `packages/core/tests/modeling/date-grain-plan.test.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts` |
| Claude modeling hardening workflow | Tool responses now return concise text summaries with full payloads in `structuredContent`; star-schema apply supports `dryRun`; primitive table/relationship tools steer cross-fact joins to batch apply; governed Date create proves before mutating; non-idempotent creates refuse automatic replay after connection drops. | Prevent Claude Code from looping through low-level tools, refreshing without explicit policy, partially mutating live models, duplicating creates, or pasting raw JSON into user responses. | `docs/superpowers/plans/2026-06-09-adversarial-claude-modeling-hardening.md`; `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/model-list-tools.test.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md` |
| Semantic clarification gate | Dashboard/model workflows must ask concise clarifying questions before writes when measure intent, actual/target source, audience/decision context, date policy, grain, source-of-truth fields, optional data dictionary/glossary grounding, or visual comparison semantics are ambiguous. Draft business terms or draft measure intent keep the spec at `needs-user-input`; confirmed user/spec/dictionary context supplies business meaning, while live MCP tools prove field existence. Measure create/update tools require confirmed `measureIntent`, model-resolved source refs, and model-resolved time-intelligence Date evidence for time-intelligence DAX. | Prevent arbitrary formulas, Date ranges, wrong target sources, wrong grain, unconfirmed KPI semantics, and visuals or measures that look valid but answer the wrong question. | `docs/data-dictionary.md`; `README.md`; `skills/authoring-measures/references/measure-intent-contract.md`; `skills/pbi-init-config/SKILL.md`; `skills-report/planning-dashboards/SKILL.md`; `skills-report/planning-dashboards/references/intake-protocol.md`; `agents/data-analyst.md`; `agents/model-builder.md`; `agents-report/report-builder.md`; `packages/mcp/src/server.ts`; `packages/mcp/tests/claude-contracts.test.ts`; `packages/mcp/tests/tool-registry.test.ts` |
| PBIR report persistence | Report/page/visual tools write PBIR files on disk and return a disk-persistence warning. Agents must not tell users to press Ctrl+S from an already-open stale Desktop report after PBIR disk writes; reopen/reload the `.pbip` first. | Prevent Desktop save from overwriting report visuals written to disk. | `packages/mcp/src/server.ts`; `packages/mcp/tests/tool-registry.test.ts`; `packages/mcp/tests/claude-contracts.test.ts`; `agents-report/report-builder.md`; `skills-report/pbi-report/SKILL.md` |
| Star schema planner/apply | Actuals/targets-style joins first use `pbi_model_plan_actuals_targets_join`, which separates non-temporal shared axes from temporal Date axes and batches date-grain proof. Other cross-fact joins plan with `pbi_model_plan_star_schema_join`; live writes use `pbi_model_apply_star_schema_join` with explicit axes, or `dryRun: true` for a no-write executable sequence. Manual replay is forbidden; apply re-plans, normalizes live data-type casing before axis/relationship gates, refuses stale or unknown-cardinality repair targets, reuses conformed dimensions by proven relationship/key coverage, and validates final state. | Prevent fact-to-fact relationships, duplicate dimensions, source-FK key mutation, stale relationship repair, skipped FK hiding, fragile TREATAS bridges, low-level tool loops, and agents asking grain questions before observable proof. | `packages/core/src/modeling/star-schema-plan.ts`; `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/core/tests/modeling/star-schema-plan.test.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `packages/mcp/tests/claude-contracts.test.ts`; `skills/modeling-semantic-model/references/columns-relationships.md`; `agents/data-analyst.md`; `agents/model-builder.md`; `agents/model-reviewer.md`; `skills/modeling-semantic-model/SKILL.md` |
| Star schema partial-run safety | Batch star-schema apply refuses partial generated dimension artifacts before creating duplicates, even when live metadata lacks source DAX, and preflights stale relationship-repair/create gates before any generated-table, refresh, key metadata, or relationship writes. | Prevent retry loops from producing duplicate shared dimensions or partial metadata writes after interrupted, stale, or mixed live/disk workflows. | `packages/mcp/src/server.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `skills/modeling-semantic-model/references/columns-relationships.md`; `agents/model-builder.md` |
| Star schema key validation | Batch star-schema apply fails closed when generated dimension key metadata is missing or incomplete, including missing `summarizeBy`, missing key flag, unknown type, or visible source FK fields. | Prevent relationships/visuals that load but expose duplicate fields or unstable dimension keys. | `packages/mcp/src/server.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `skills/modeling-semantic-model/references/columns-relationships.md`; `agents/model-builder.md` |
| Duplicate field hiding | Planned `hideFkWrites` are part of build completion. Visible fact-side FK fields that duplicate visible dimension/source fields escalate to `MOD005` error and can block visual binding. | Keep dimensions as the report-author source of truth and prevent slicers that filter only one fact. | `packages/core/src/modeling/bpa.ts`; `packages/core/tests/modeling/bpa.test.ts`; `packages/core/tests/visual/bind-validator.test.ts`; `agents/model-builder.md`; `skills/reviewing-models/references/check-catalog.md` |
| Multi-instance targeting | Live model tools accept `model` selectors by name or port and avoid guessed model-name retry loops. | Prevent writes going to the wrong open Desktop instance and reduce latency. | `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/model-builder.md` |
| Snapshot/cache safety | Live snapshots are connection-bound, option-aware, and write-locked; driver writes support batched column/relationship definitions. | Prevent checks using another model's metadata or a measure-excluded snapshot, and reduce per-object list/write loops. | `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-driver.test.ts` |
| Model check expansion | `pbi_model_check` now covers more BPA-style correctness, DAX, formatting, naming, maintenance, date, and relationship rules. Findings must be attributed to tool output. | Reduce soft-prompt reviews and catch silent wrong-number conditions. | `packages/core/src/modeling/bpa.ts`; `packages/core/tests/modeling/bpa.test.ts`; `skills/reviewing-models/references/check-catalog.md`; `agents/model-reviewer.md` |
| Regulated model readiness | `pbi_model_regulated_check` reports captured vs not-captured metadata and blocks regulated/Copilot readiness when RLS, OLS, sensitivity, lineage, governance, AI-scope, leakage-test, tenant-setting, or signoff evidence is missing. RLS is read/evidence-only in the beta: no role authoring, assignment, View-as testing, or leakage proof. | A clean structural model check is not a bank-safe launch signal, and RLS evidence capture is not RLS certification. | `docs/modeling-beta-features.md`; `packages/core/src/modeling/doctor.ts`; `packages/core/src/modeling/types.ts`; `packages/core/src/modeling/tmdl-parser.ts`; `packages/core/tests/modeling/doctor.test.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/tool-registry.test.ts` |
| Visual binding guard | Visual binding validation blocks model-side BPA errors that affect bound fields, hidden superseded fields, bridge-blocked axes, and missing filter paths. | Prevent visuals from binding to wrong fields even when the model technically loads. | `packages/core/src/visual/bind-validator.ts`; `packages/core/tests/visual/bind-validator.test.ts`; `skills-report/reviewing-reports/references/check-catalog.md` |

## Maintenance Rules

- Keep this doc dataset-agnostic. Do not add demo table or column names.
- Prefer replacing stale entries over adding duplicates.
- Delete contradicted entries when the newer behavior is implemented and verified.
- Every entry must include reference files.
- Keep each row short enough to scan.

## Local Maintenance Skill

Dev-only skill kept out of the auto-scanned beta surface: `skills-internal/system-improvements/SKILL.md`. Read and follow it directly (it is not loaded as a plugin skill in the modeling beta).
