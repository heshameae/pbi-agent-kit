# System Improvements

Living log of behavior and logic hardening work. Keep entries short. Update or delete stale entries when a newer decision supersedes them.

## Active Improvements

| Area | Improvement | Why | Reference files |
|---|---|---|---|
| Live model workflow | Model writes are live-first. Omit `folderPath` when Desktop is open; live changes appear immediately and persist with Ctrl+S. Folder mode is offline fallback only. | Prevent stale disk edits, close/reopen workflows, and edits landing in the wrong model. | `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md`; `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts` |
| Live model discovery freshness | User-facing model reads use fresh live snapshots by default, redact connection strings, and return remediation guidance when targeted live tables are missing. Ctrl+S is not presented as the only remedy; agents should check target instance, Power Query apply/materialization, live refresh, and reopen-after-disk-edit cases. | Prevent stale cache/target confusion from hiding newly-added Desktop tables and giving misleading persistence-only advice. | `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-list-tools.test.ts`; `packages/mcp/tests/model-driver.test.ts`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md` |
| Modeling-only beta surface | Dashboard/report/page/visual/PBIR authoring is unavailable in this modeling-only beta. Scope is enforced by directory isolation and source-tree cleanup: only the 5 modeling skills live under `skills/`, only the 3 modeling agents under `agents/`, and report/PBIR source, archives, demos, and full-surface config are absent from `main`. The marketplace installs `.claude-plugin/plugin.json`, which carries only `mcpServers: ./.mcp.json` and relies on default scanning of `skills/` and `agents/`. No explicit `agents` file-path array is used because it suppressed default discovery and loaded zero agents in this Claude Code version. The MCP launcher compares a source/package fingerprint marker stamped only by the root workspace build, writes a normal non-hidden marker filename with read-only tolerant replacement, and logs the loaded build hash/time. `UserPromptSubmit`/`UserPromptExpansion`/`PreToolUse` hooks remain a defense-in-depth backstop that refuses report prompts, stale report skill/agent expansion, and report/PBIR tool calls. | A manifest allowlist does not scope a plugin: only shipped files are discoverable, so report behavior must be physically absent from `main`, not hidden by config. | `.mcp.json`; `scripts/start-mcp.mjs`; `scripts/build-fingerprint.mjs`; `scripts/write-build-marker.mjs`; `.claude-plugin/plugin.json`; `.claude-plugin/marketplace.json`; `README.md`; `hooks/hooks.json`; `hooks/scripts/guard-modeling-beta-scope.mjs`; `packages/mcp/src/server.ts`; `packages/mcp/tests/claude-contracts.test.ts`; `packages/mcp/tests/plugin-inventory.test.ts` |
| No raw file surgery | Agents must not hand-edit TMDL/PBIP/Report/CSV artifacts by default; hooks allow only explicit repo fixtures/templates, block shell/Node file surgery, block alias-agnostic direct Microsoft MCP write bypasses, and docs point users at the wrapper rather than a raw peer server. Unsupported operations are reported as unsupported. | Desktop does not reload disk edits into the running model; raw edits bypass wrapper gates. | `README.md`; `skills/pbi-init-config/SKILL.md`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md`; `hooks/hooks.json`; `hooks/scripts/guard-no-python-powerbi-ops.mjs`; `packages/mcp/tests/claude-contracts.test.ts`; `packages/mcp/src/server.ts` |
| No Python operations | Python is blocked for pbi-agent-kit operations, including absolute/venv interpreter paths, data probing, CRLF patches, TMDL rewrites, and CSV inspection. Use MCP tools or repo-native TypeScript/Node. | Prevent ad hoc scripts from bypassing deterministic model gates. | `hooks/hooks.json`; `hooks/scripts/guard-no-python-powerbi-ops.mjs`; `packages/mcp/tests/claude-contracts.test.ts`; `AGENTS.md`; `CLAUDE.md` |
| Date table planning | Existing Date table edits, marking, and diagnosis require `pbi_model_plan_date_table`; new Date/Calendar tables use `pbi_date_table_create_governed`. Literal calendar bounds and volatile anchors like `TODAY()` are rejected unless an explicit future horizon policy is supplied. | Prevent hardcoded ranges, missing years, blank rows, and date logic drifting with system date. | `packages/core/src/modeling/date-grain-plan.ts`; `packages/core/src/modeling/bpa.ts`; `packages/core/tests/modeling/date-grain-plan.test.ts`; `packages/core/tests/modeling/bpa.test.ts`; `skills/reviewing-models/references/check-catalog.md` |
| Governed Date table create | New Date/Calendar tables use `pbi_date_table_create_governed`, which asks for Date policy and refresh-before-probe policy when ambiguous, proves fact-date evidence before any write, refreshes before proof only when `refreshBeforeProbe:true` is explicit, derives the complete fact-date set before generating dynamic fact-anchored DAX, caps future-horizon ends at the explicit horizon, hardens generated-column metadata, marks the table, and creates Date relationships automatically when proof allows them. Import models may reject the `isKey` column write; a successful table `dataCategory:Time` mark plus live unique/contiguous Date-key proof is accepted. For multiple date roles on the same fact table, governed create makes the first role active and later roles inactive. Blocked/incomplete Date proof stops the workflow; parse-shape-unrecognized/evidenceRows:0 from a ROW-based proof is a tool defect to report, not a no-data finding. Do not use `pbi_dax_query`, `pbi_model_refresh`, manual DAX, `probeData:false`, or primitive Date/relationship writes as fallbacks. | Replace prompt-generated `CALENDAR(DATE(...), DATE(...))`, `TODAY()` anchors, manual DAX entry, manual metadata edits, manual mark/relationship steps, Auto Date/Time cleanup loops, ungated refresh/retry loops, too-narrow calendars, unsafe monthly-target Date relationships, and empty-model misdiagnoses from unparsed proof shapes. | `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md`; `skills/modeling-semantic-model/references/tmdl-grammar.md` |
| Date grain planning | Before asking target grain/day/month/year questions, target/budget/forecast workflows require proof first. Actuals/targets-style joins start with `pbi_model_plan_actuals_targets_join`; other grain-sensitive workflows call `pbi_model_plan_date_grain`. `writePlan` / `plainSumSafe` are action-safe for writes and measure rewrites. Sparse but complete day-level, nonblank, date-only proof can be `day-or-above`, allow Date relationships, and mark plain additive measures safe even when exact contiguous fact-date proof is missing; `observedGrain` is evidence for observable date grain, while allocation and missing-target behavior remain user semantics after proof. Blocked, incomplete, or parse-shape-unrecognized proof is a stop condition with structured `blockedAction`, forbidden fallback tools, and forbidden `probeData:false` retry guidance. | Prevent unnecessary grain questions, monthly/daily grain assumptions, repeated target values, over-blocking sparse transactional day visuals/relationships, DateTime relationships that pass date-only coverage but fail exact joins, disconnected Date tables that repeat grand totals by Year, and fallback loops after proof defects. | `packages/mcp/src/server.ts`; `packages/core/src/modeling/date-grain-plan.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `packages/core/tests/modeling/date-grain-plan.test.ts`; `packages/mcp/tests/claude-contracts.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/data-analyst.md`; `agents/model-builder.md`; `agents/model-reviewer.md`; `skills/modeling-semantic-model/SKILL.md`; `skills/modeling-semantic-model/references/grain.md`; `skills/authoring-measures/SKILL.md` |
| Live refresh tooling | `pbi_model_refresh` refreshes/processes the live model when Import data or calculated tables must materialize and refresh is explicitly approved. If approval is missing, agents ask for refresh authorization and stop; Ctrl+S is persistence only. It is forbidden as a fallback after Date proof parse-shape defects or Date proof blockers that instruct STOP. | Prevent workflows from stopping midway, asking users to click Desktop Refresh unnecessarily, refreshing unexpectedly, or treating a parser defect as stale data. | `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md` |
| Date relationship gates | Temporal relationship creates/updates/activations require governed Date table coverage, proven date-only day-grain facts, explicit future-horizon policy, and single-direction filtering. The endpoint check is the mark-INDEPENDENT `looksLikeDateRelationshipEndpoint` (temporal, non-auto column; NO `dataCategory:Time` read-back), and the coverage check uses `coverage.markReadiness.ready` (drops the cosmetic `date-table-not-marked`/`date-column-not-key` blockers when the key is data-proven; keeps every real proof blocker). So a no-op metadata mark cannot block a relationship whose key is proven from data, while uniqueness/continuity/coverage are still enforced. The CORE planner layer is decoupled to MATCH the gate: `buildWritePlan` keys off `coverage.markReadiness.ready` (not `coverage.status==='valid'`) and `relationshipTargetsGovernedDateEndpoint` accepts a non-auto temporal endpoint whose key is data-proven (not only `isMarkedDateTable`), so an ACTIVE create/activate no longer deadlocks on an Import no-op mark — previously only the gate's outer layers were decoupled while the planner still required the mark, re-introducing the deadlock for the active case. Sparse fact dates are valid; Date-key continuity is proven on the Date table. Inactive role-playing Date relationships can be created after proof. FAIL-OPEN-ON-UNOBSERVABLE: the old `calendar-source-proof-missing` blocker (refused when `table.expression`/`partitionSources` were absent) is now a non-blocking `calendar-source-unproven` WARNING relayed on `planDateTableCoverage().warnings` and surfaced by the mark/relationship gates (`warnings`/`dateTableWarnings`). `partitionSources` is never populated on a live Import read and a Power-Query Date table has no table-level expression, so absence of the source is structurally unobservable, NOT a defect — treating it as one made `keyProvenFromData` self-arm the refusal and deadlock mark-as-date, date relationships, and YTD/QTD/MTD on every clean Import Date table. A POSITIVELY-OBSERVED volatile `TODAY()/NOW()` anchor or hardcoded literal range still BLOCKS via `findDateTableSourceRisks` (it reads an actual source string); the realized-key probe (blanks/dups/gaps/non-midnight/coverage/end-after-fact-max) remains the enforced safety. EVERY layer now agrees on the data-proven signal, not the unobservable mark: (a) the read-only readiness planner `planActualsTargetsJoin` keys off `coverage.markReadiness.ready` / `blockingForMark` (was `coverage.status!=='valid'`, which re-blocked a clean unmarked Import Date table and steered the agent to recreate it); (b) the governed-axis DETECTION `inferCommonGovernedDateEndpoint` uses the mark-independent `looksLikeDateRelationshipEndpoint` (was `looksLikeGovernedDateEndpoint`, which required the unobservable `dataCategory:Time` mark, so an unmarked-but-fact-related real Date table was reported `governed-date-table-required`); and (c) the `calendar-source-unproven` caveat is relayed on the relationship update/activate (`dateTableWarnings`), actuals/targets readiness (`dateTableWarnings`), and star-schema create paths, not silently dropped. | Prevent invalid date joins, disconnected governed Date tables that repeat totals, unapproved future calendar padding, and ambiguous temporal filters while allowing normal sparse transactional facts — and stop a cosmetic, silently-no-op `dataCategory:Time` mark OR an unreadable Import calendar source from deadlocking a fully data-proven join at the gate or the planner layer, while still relaying an honest caveat. | `packages/mcp/src/server.ts`; `packages/core/src/modeling/date-grain-plan.ts`; `packages/core/src/modeling/relationship-check.ts`; `packages/core/tests/modeling/relationship-check.test.ts`; `packages/core/tests/modeling/date-grain-plan.test.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts` |
| Live DAX probe result handling | `daxQuery` normalizes the wrapped modeling MCP's columnar Execute envelope (`data.columns` or nested `data.results[].tables[]` + positional `rows` array-of-arrays in direct or nested `content[].text`, plus beta.2 `content[].resource` CSV result files next to `{success:true}` acknowledgements) into rows keyed by column name; malformed tabular rows throw instead of reading as empty. Engine errors returned without MCP `isError` (`success:false` / non-tabular text) throw, while tabular payloads are preferred over plain text acknowledgements; probe blocks/incomplete paths surface `rowCount`/`columns`/`sampleRow` diagnostics. Expected ROW-based proof rows with zero parsed evidence return `parse-shape-unrecognized` / `proof-parse-shape-unrecognized`, even when driver diagnostics report `rowCount:0`; proof callers opt into bounded structure-only `rawDiagnostics` so new host shapes can be diagnosed without leaking values. Partial parsed evidence can still return missing-proof blockers. Generated probe DAX must stay parenthesis-balanced (regression-tested) since the driver is mocked in unit tests and never executes it. | The live DAX result shape is not the public Execute-Queries REST array-of-objects shape. In the Microsoft modeling MCP beta.2 path, `content[0].text` can be only `{success:true}` while the actual tabular rows live in a sibling `content[].resource` CSV; other hosts may use text wrappers or structured envelopes. Treating the acknowledgement as the result silently returned zero rows and masked engine/syntax errors, blocking date-table, mark-as-date, and temporal relationship proofs. ROW-based proof parse failures must be reported as tool defects rather than empty data. | `packages/mcp/src/model-bridge/model-driver.ts`; `packages/core/src/modeling/date-grain-plan.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/model-list-tools.test.ts`; `packages/core/tests/modeling/date-grain-plan.test.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts` |
| Claude modeling hardening workflow | Tool responses return concise text summaries with full payloads in `structuredContent`; star-schema apply supports `dryRun`; primitive table/relationship tools steer cross-fact joins to batch apply; governed Date create proves before mutating; non-idempotent creates refuse automatic replay after connection drops. | Prevent Claude Code from looping through low-level tools, refreshing without explicit policy, partially mutating live models, duplicating creates, or pasting raw JSON into user responses. | `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/model-list-tools.test.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/model-builder.md`; `skills/modeling-semantic-model/SKILL.md` |
| Semantic clarification gate | Dashboard/model workflows must ask concise clarifying questions before writes when measure intent, actual/target source, audience/decision context, date policy, grain, source-of-truth fields, optional data dictionary/glossary grounding, or visual comparison semantics are ambiguous. Draft business terms or draft measure intent keep the spec at `needs-user-input`; confirmed user/spec/dictionary context supplies business meaning, while live MCP tools prove field existence. Measure create/update tools require confirmed `measureIntent`, model-resolved source refs, and model-resolved time-intelligence Date evidence for time-intelligence DAX. | Prevent arbitrary formulas, Date ranges, wrong target sources, wrong grain, unconfirmed KPI semantics, and measures that look valid but answer the wrong question. | `docs/data-dictionary.md`; `README.md`; `skills/authoring-measures/references/measure-intent-contract.md`; `skills/pbi-init-config/SKILL.md`; `agents/data-analyst.md`; `agents/model-builder.md`; `packages/mcp/src/server.ts`; `packages/mcp/tests/claude-contracts.test.ts`; `packages/mcp/tests/tool-registry.test.ts` |
| Star schema planner/apply | Actuals/targets-style joins first use `pbi_model_plan_actuals_targets_join`, which separates non-temporal shared axes from temporal Date axes and batches date-grain proof. A temporal actuals/targets axis is ready only when every fact shares one governed Date table; `LocalDateTable_*` relationships are not sufficient and return `dateAxisRequirement: governed-date-table-required` with `pbi_date_table_create_governed` as the path. Other cross-fact joins plan with `pbi_model_plan_star_schema_join`; generic plan/apply now also surfaces `dateAxisRequirement` when the participating tables expose shared temporal axes, and `shared-dimensions-*-date-axis-incomplete` means categorical dimensions were handled but the governed Date axis still must be created/proven. Live writes use `pbi_model_apply_star_schema_join` with explicit axes, or `dryRun: true` for a no-write executable sequence. Manual replay is forbidden; apply re-plans, normalizes live data-type casing before axis/relationship gates, refuses stale or unknown-cardinality repair targets, reuses conformed dimensions by proven relationship/key coverage, and validates final state. | Prevent fact-to-fact relationships, duplicate dimensions, source-FK key mutation, stale relationship repair, skipped FK hiding, fragile TREATAS bridges, disconnected Date tables that repeat grand totals by Year, categorical-only "done" claims while Date axes remain local/disconnected, low-level tool loops, and agents asking grain questions before observable proof. | `packages/core/src/modeling/star-schema-plan.ts`; `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/core/tests/modeling/star-schema-plan.test.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `packages/mcp/tests/claude-contracts.test.ts`; `skills/modeling-semantic-model/references/columns-relationships.md`; `agents/data-analyst.md`; `agents/model-builder.md`; `agents/model-reviewer.md`; `skills/modeling-semantic-model/SKILL.md` |
| Star schema partial-run safety | Batch star-schema apply refuses partial generated dimension artifacts before creating duplicates, even when live metadata lacks source DAX, and preflights stale relationship-repair/create gates before any generated-table, refresh, key metadata, or relationship writes. | Prevent retry loops from producing duplicate shared dimensions or partial metadata writes after interrupted, stale, or mixed live/disk workflows. | `packages/mcp/src/server.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `skills/modeling-semantic-model/references/columns-relationships.md`; `agents/model-builder.md` |
| Star schema key validation | Batch star-schema apply fails closed when generated dimension key metadata is missing or incomplete, including missing `summarizeBy`, missing key flag, unknown type, or visible source FK fields. | Prevent relationships/visuals that load but expose duplicate fields or unstable dimension keys. | `packages/mcp/src/server.ts`; `packages/mcp/tests/model-star-schema-plan.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `skills/modeling-semantic-model/references/columns-relationships.md`; `agents/model-builder.md` |
| Duplicate field hiding | Planned `hideFkWrites` are part of build completion. Visible fact-side FK fields that duplicate visible dimension/source fields escalate to `MOD005` error. | Keep dimensions as the source of truth and prevent downstream slicers from filtering only one fact. | `packages/core/src/modeling/bpa.ts`; `packages/core/tests/modeling/bpa.test.ts`; `agents/model-builder.md`; `skills/reviewing-models/references/check-catalog.md` |
| Multi-instance targeting | Live model tools accept `model` selectors by name or port and avoid guessed model-name retry loops. | Prevent writes going to the wrong open Desktop instance and reduce latency. | `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/tests/tool-registry.test.ts`; `agents/model-builder.md` |
| Snapshot/cache safety | Live snapshots are connection-bound, option-aware, and write-locked; driver writes support batched column/relationship definitions. | Prevent checks using another model's metadata or a measure-excluded snapshot, and reduce per-object list/write loops. | `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-driver.test.ts` |
| Model check expansion | `pbi_model_check` now covers more BPA-style correctness, DAX, formatting, naming, maintenance, date, and relationship rules. Findings must be attributed to tool output. | Reduce soft-prompt reviews and catch silent wrong-number conditions. | `packages/core/src/modeling/bpa.ts`; `packages/core/tests/modeling/bpa.test.ts`; `skills/reviewing-models/references/check-catalog.md`; `agents/model-reviewer.md` |
| Regulated model readiness | `pbi_model_regulated_check` reports captured vs not-captured metadata and blocks regulated/Copilot readiness when RLS, OLS, sensitivity, lineage, governance, AI-scope, leakage-test, tenant-setting, or signoff evidence is missing. RLS is read/evidence-only in the beta: no role authoring, assignment, View-as testing, or leakage proof. | A clean structural model check is not a bank-safe launch signal, and RLS evidence capture is not RLS certification. | `docs/modeling-beta-features.md`; `packages/core/src/modeling/doctor.ts`; `packages/core/src/modeling/types.ts`; `packages/core/src/modeling/tmdl-parser.ts`; `packages/core/tests/modeling/doctor.test.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/tool-registry.test.ts` |
| Live write verification | Live write tools confirm success by a fresh live re-read, not the bridge's acknowledgement. The driver throws on `{success:false}` envelopes and on batched `{results:[{success:false}]}`; column/table-property writes re-read the same connection and fail explicitly when requested ≠ actual — EXCEPT `isKey` and `isHidden`, which stay best-effort and are NEVER asserted on read-back (both carry the Import-mode false-negative hazard: the live List may omit the key, which parses to `false`, so a write that landed reads back as `false`). `markAsDateTable` returns `dataCategoryNotApplied` when the `dataCategory:Time` re-read does not reflect it, and the standalone `pbi_table_mark_as_date` now degrades that to a NON-BLOCKING result (`marked:false` + actionable `warning`) instead of throwing — the date key is proven from data, so the mark is best-effort and never deadlocks downstream work. `pbi_date_table_create_governed` likewise reports `markWarning` and stays `created` (status via `coverage.markReadiness.ready`, not `coverage.status`) when only the cosmetic mark no-ops. Deleting/deactivating a relationship that would orphan a column's date axis (auto `LocalDateTable` Variation on delete; a fact's only active date relationship on deactivate → `date-relationship-orphan`) is refused. `updateMeasure` (expression/format/description), `updateRelationship` (endpoints/cardinality/crossFilter/isActive), and `activateRelationship` (isActive) now route through the same `#writeVerified` read-back (cannot-disprove when the object is unobservable; an empty-string measure expression read-back is treated as unobservable, not contradiction) — closing the gap where those property updates trusted the bridge ack and could report `updated:true` on an Import no-op. The governed Date-table policy annotation reports `stamped:true, verified:false` with a note (annotations are not asserted on read-back; UNVERIFIED wire shape) so the agent does not over-trust an unconfirmed stamp. | Table/column writes silently returned success while the property never persisted (live Import-mode `dataCategory:Time`/`isHidden`), so agents invented a false "Import tables can't be marked" rule, asked the user to hide already-hidden fields, and fell back to manual Desktop steps; the earlier hard `date-category-write-not-applied` block then deadlocked the join and a deactivate-before-create left the model with no date axis. | `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/model-driver.test.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `skills/modeling-semantic-model/SKILL.md`; `agents/model-builder.md` |
| Time-intelligence range policy | A future-horizon Date table (calendar extends past the last fact date) makes default-context (no-slicer) `TOTALYTD/QTD/MTD` return BLANK over the empty tail. `pbi_date_table_create_governed` reframes the range-policy clarifying question so historical analysis defaults to `observed-min-max` (calendar ends at real data) and reserves `*-plus-future-horizon` for explicit forecasting, and returns a `futureHorizonWarning` when a future-horizon policy is used. Hand-authored "clamp to the max fact year" guards are forbidden (they over-clamp filtered contexts and break drill-down); the canonical, dataset-agnostic last-data-period pattern (`buildTimeIntelligenceMeasureExpression`) caps the UPPER bound only, at day grain, per measure from that measure's own fact date column, without `REMOVEFILTERS` on a user date slicer. This is now ENFORCED deterministically at write time: `pbi_measure_create`/`pbi_measure_update` parse a bare `TOTAL*TD` (`parseBarePeriodToDate`), run a live max-vs-max probe (Date-table max vs THIS measure's fact max from `timeIntelligence.dateRefs`, via `calendarOvershootsFactDay`), and on overshoot REFUSE with `time-intelligence-default-context-blank-risk` plus the generated capped `correctedExpression`. This covers the multi-fact case (one shared Date table spanning the union of fact max-dates) that `observed-min-max` alone cannot fix. | A governed Date table extended past the data (directly, or as the union max across multiple facts) and broke default-context time intelligence; the agent hand-wrote plain `TOTALYTD`, which came back BLANK, or patched it with a fragile max-year clamp instead of the per-fact day-grain cap. | `packages/core/src/modeling/time-intelligence-plan.ts`; `packages/core/tests/modeling/time-intelligence-plan.test.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts`; `skills/authoring-measures/SKILL.md`; `skills/modeling-semantic-model/SKILL.md`; `agents/model-builder.md` |
| Determinism/accuracy hardening (audit) | Cross-gate consistency: `MAX_FUTURE_HORIZON_DAYS` is single-sourced in `pbi-core` and clamped inside `readGovernedDatePolicy`, so the mark gate and relationship gate can no longer diverge on a tampered/oversized persisted horizon. The blank-risk TI guard no longer fails open SILENTLY on a probe error — it returns an explicit warning. BPA enforcement scope is labeled (ENFORCED DAX001–014/MOD*/FMT*/NAM* vs ADVISORY DAX015+/QRY*/MDL*/DL*) so agents do not over-trust un-checked codes. Phantom-tool guidance removed: the `power-query` skill and DAX trace-optimization protocol no longer instruct calling tools that do not exist in the modeling-only beta (executeQuery/XMLA/updateDefinition, trace timings, `INFO.VIEW.MEASURES()`); hand-authored TI templates point to the deterministic generator; ai-readiness points to `pbi_dax_reference_check` not Python. A deterministic CI test (`agent-skill-reconciliation.test.ts`) fails when an agent loads a non-existent skill or when any agent/skill frontmatter names an MCP tool the server does not register. Day-grain heuristic thresholds are named constants. | A 15-agent adversarial audit found gate/prompt determinism footholds: a hardcoded cross-gate divergence, a silent fail-open, advisory codes masquerading as enforced, skills instructing non-existent tools, and stale skill/agent/tool references that fail only at runtime and push agents to improvise. | `packages/core/src/modeling/date-grain-plan.ts`; `packages/mcp/src/server.ts`; `packages/mcp/tests/agent-skill-reconciliation.test.ts`; `skills/power-query/`; `skills/authoring-measures/`; `skills/reviewing-models/SKILL.md`; `skills/modeling-semantic-model/references/ai-readiness.md`; `agents/model-reviewer.md`; `agents/model-builder.md`; `agents/data-analyst.md` |
| Data-proven Date key + persisted policy | Date-key readiness is proven from probe data (`isDataProvenDailyKey`: complete, unique `distinct==nonBlank`, gap-free, non-blank, midnight-only) rather than the `isKey` metadata flag, so a `dataCategory=Time` Date table marks and joins even when the `isKey` write is rejected. `markReadiness.blockingForMark` single-sources the allowed pre-mark blockers (drops `date-table-not-marked`/`date-column-not-key` only when the key is data-proven; every real proof blocker holds). `pbi_date_table_create_governed` stamps `governedByTool`/range/horizon annotations on the Date table; `pbi_table_mark_as_date` short-circuits the coverage re-probe for an already-governed marked table, and the mark/temporal-relationship gates read the persisted horizon instead of re-requiring `futureHorizonDays` or re-blocking on `date-table-end-after-fact-max`. The annotation write/read wire shape against the live MS modeling MCP is UNVERIFIED — gated behind try/catch and degrades to prior behavior, never blocking. | isKey-coupled gates and an unpersisted horizon made governed Date tables uncreatable end-to-end on live Import models. | `packages/core/src/modeling/date-grain-plan.ts`; `packages/core/src/modeling/types.ts`; `packages/core/src/modeling/tmdl-parser.ts`; `packages/mcp/src/server.ts`; `packages/core/tests/modeling/date-grain-plan.test.ts`; `packages/mcp/tests/model-date-grain-plan.test.ts` |
| Determinism/accuracy hardening (audit wave 2) | A second adversarial audit drove these dataset-agnostic fixes. **Determinism:** the TMDL folder parser sorts tables/roles by code-point name (`compareByName`), `runBPA` imposes a canonical `(ruleId,object,message)` sort, DAX008 sorts its peer-measure list, and the data dictionary + `pbi_model_list_tables` sort by name — so identical models yield byte-identical output regardless of filesystem readdir order. **Consistency:** `MOD014` fires on implicit-Sum numeric keys via a shared `isAggregatableNumeric` (engine-default Sum: undefined `summarizeBy` on a numeric = aggregatable, only explicit `none` is not); `naming.ts` is the single home for the canonical `looksLikeKeyName` (bpa/star-schema) + `compareByName`; the fact-classifier counts fan-out only on many-side edges (a oneToMany conformed dimension is no longer misclassified as a fact); `validateBridge` gates shared axes on type compatibility (still via `dimColumnsOf(to)` so hidden TO columns stay excluded). IMPORTANT — the adversarial VERIFICATION round below caught that unifying fact-classifier/grain/field-index onto the engine-default-Sum notion was WRONG: it made a dimension carrying a numeric attribute (Weight/Latitude/FiscalYear, no `summarizeBy`) misclassify as a fact and dropped numeric surrogate keys from the grain/axis set. So those three KEEP the conservative explicit-`summarizeBy` signal; only star-schema's axis test uses the engine-default-Sum `isMeasureLikeNumeric`. The two notions are intentionally DISTINCT. **Wrong-numbers (TI):** the blank-risk gate refuses to auto-rewrite a `TOTAL*TD` carrying extra args (re-threads only a recognized fiscal year-end literal, else refuses with a manual-cap instruction rather than silently dropping a filter/year-end), refuses on a date-axis mismatch between expression and intent, strips leading DAX comments, detects a top-level `<=` cap (not a `<=` inside the base aggregate), and selects the overshoot fact deterministically by base-expression table match. **Locale-safety:** probe count cells coerce only via a strict integer parser (European "1.234" fails closed, not silently → 1.234) and date cells parse ISO-only (no `Date.parse` MM/DD misread). **Honesty:** `pbi_model_regulated_check` requires as HARD metadata only what the assemblers actually produce (RLS roles); dataSources/sensitivity/lineage/governance are attested via policy evidence (not an unsatisfiable capture flag), so `passed` is reachable. **Phantom/collision:** the performance-pattern catalog is renamed to the `PERF###` namespace so it cannot collide with the bpa.ts-enforced `DAX###` rule ids (an agent could otherwise apply the wrong remediation to a `pbi_model_check` finding); `INFO.VIEW.MEASURES()` DMV-probe guidance removed; dangling `object-properties-full.md` ref and a hardcoded `'Date'[DateKey]` example fixed; `pbi_dax_query` added to `authoring-measures` allowed-tools; the read-only `data-analyst` offer no longer claims write/refresh verbs. **Gates:** `pbi_model_refresh` requires explicit `confirmReprocess:true` (a reprocess changes every measure value); calc-column create/update run a best-effort advisory `daxReferenceCheck` (`referenceAdvisory`, never blocks). New CI guards: the reconciliation test fails on any `INFO.VIEW.` reference or any `DAX###` token under `authoring-measures` (PERF-namespace lock). DEFERRED (documented, not done): making `isHidden`/`isKey`/`isCalculated` nullable in the LIVE read shape so BPA distinguishes absent-from-Import-List vs explicit-false (largest blast radius across the whole pipeline; the premise that the live List omits these on read is UNVERIFIED on Windows — pursue with a live read-shape check first); leaving a measure expression `undefined` rather than `''` (same nullable-type cluster); a create-class existence re-read in `assertNoBatchFailure`; the two-active-date-relationships-on-one-FK active/inactive refinement; and a coverage gate for integer-surrogate (non-temporal) date keys. | A 15-agent refute-by-default audit confirmed 36 findings across hallucination, wrong-numbers, date-relationships, honesty, and determinism; two follow-up adversarial VERIFICATION rounds then re-checked every fix against the actual code: round 1 confirmed 8 regressions/holes from the first pass (the fact/grain over-classification above, a hidden-TO-column bridge regression, an unquoted-table-name axis-mismatch false-refusal, a calendar-source-proof hole for unmarked-but-data-proven tables, a value-filter `<=` mis-detected as a date cap, and a discarded blank-risk probe-failure warning); round 2 confirmed 3 narrower ones from those corrections (`normalizeDaxRef` over-stripping quotes/whitespace so distinct table names could collide → silent calendar swap; `grain.isAggregatedNumericColumn` case-sensitivity vs its case-insensitive siblings dropping a capitalized `summarizeBy: None` numeric; and a stale `isAggregatableNumeric` doc comment). round 3 confirmed 1 LOW (the `normalizeDaxRef` axis compare was case-SENSITIVE, but DAX identifiers are case-insensitive, so a same-axis measure authored with different casing was falsely refused — now lower-cased; the NUL-byte separator was also removed in favor of unambiguous straight concatenation). All corrected; finding counts narrowed each round (36 → 8 → 3 → 1) with no HIGH/critical after the first pass, indicating convergence. This row records the implemented, test-backed, dataset-agnostic fixes (375 core + 341 mcp tests pass) and the consciously-deferred items with their reasons. | `packages/core/src/modeling/naming.ts`; `packages/core/src/modeling/data-types.ts`; `packages/core/src/modeling/tmdl-parser.ts`; `packages/core/src/modeling/bpa.ts`; `packages/core/src/modeling/grain.ts`; `packages/core/src/modeling/fact-classifier.ts`; `packages/core/src/modeling/field-index.ts`; `packages/core/src/modeling/star-schema-plan.ts`; `packages/core/src/modeling/data-dictionary.ts`; `packages/core/src/modeling/date-grain-plan.ts`; `packages/core/src/modeling/time-intelligence-plan.ts`; `packages/core/src/modeling/doctor.ts`; `packages/mcp/src/server.ts`; `packages/mcp/src/model-bridge/model-driver.ts`; `packages/mcp/tests/agent-skill-reconciliation.test.ts`; `skills/authoring-measures/`; `agents/data-analyst.md`; `agents/model-builder.md` |

## Maintenance Rules

- Keep this doc dataset-agnostic. Do not add demo table or column names.
- Prefer replacing stale entries over adding duplicates.
- Delete contradicted entries when the newer behavior is implemented and verified.
- Every entry must include reference files.
- Keep each row short enough to scan.
